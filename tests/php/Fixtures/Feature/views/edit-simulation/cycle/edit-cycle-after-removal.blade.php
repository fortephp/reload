<form method="POST" class="flex flex-col gap-6">
    <div>
        <span>{{ 'Email address' }}</span>
    </div>

    @if (true)
        <button type="submit"><strong>{{ 'Log in final' }}</strong></button>
    @endif
</form>
