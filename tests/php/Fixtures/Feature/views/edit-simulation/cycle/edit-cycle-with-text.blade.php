<form method="POST" class="flex flex-col gap-6">
    <div>
        <span>{{ 'Email address' }}</span>
    </div>

    asdf

    @if (true)
        <button type="submit"><strong>{{ 'Log in now' }}</strong></button>
    @endif
</form>
